# Parker Remediation — CapacityLatest sourceType Projection Fix

**Date:** 2026-04-21T16:42:19Z  
**Agent:** Parker  
**Status:** COMPLETED  
**Focus:** Fixed sourceType propagation in shared capacity view, schema/bootstrap, paged and non-paged capacity paths, added migration for existing environments

## Summary

Parker completed remediation of backend schema gaps identified by Bishop's critical review. Two findings were addressed:

1. **CapacityLatest Missing sourceType (CRITICAL)** — Updated dbo.CapacityLatest view definition to include sourceType in SELECT and PARTITION BY clause across all schema paths:
   - sql/schema.sql view definition (lines 104–143)
   - src/store/sql.js ensureSchema() embedded script
   - Migration 20260421-add-ai-model-availability.sql

2. **Paginated Query sourceType Omission (MEDIUM)** — Added sourceType to paginated capacity query SELECT list in src/services/capacityService.js (lines 293–307) to ensure cross-layer classification consistency.

## Validation

- CapacityLatest view now projects sourceType and partitions latest-row selection by sourceType to prevent AI/Compute row collapse
- Paginated and non-paginated capacity APIs both carry sourceType through row shaping
- Cross-layer resource-type classification tested with both sourceType-present and sourceType-null rows
- Migration idempotency validated; existing environments can refresh view without data loss

## Next Step

Bishop to perform re-run validation of cross-layer correctness and clear merge gate.
